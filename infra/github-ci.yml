name: ci

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: reqtrack
          POSTGRES_PASSWORD: reqtrack
          POSTGRES_DB: reqtrack_v2_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U reqtrack"
          --health-interval 5s
          --health-timeout 3s
          --health-retries 10
      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 5s
          --health-timeout 3s
          --health-retries 10
    env:
      DATABASE_URL: postgresql://reqtrack:reqtrack@localhost:5432/reqtrack_v2_test
      TEST_DATABASE_URL: postgresql://reqtrack:reqtrack@localhost:5432/reqtrack_v2_test
      REDIS_URL: redis://localhost:6379
      JWT_SECRET: ci-secret-at-least-16-chars
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 9.15.9
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm --filter @reqtrack/database validate
      - run: pnpm --filter @reqtrack/database generate
      - run: pnpm --filter @reqtrack/database build
      - run: pnpm --filter @reqtrack/config build
      - run: pnpm -r --filter "./apps/*" typecheck
      - run: bash infra/scripts/scan-forbidden-chars.sh
      - run: pnpm --filter @reqtrack/api test
      - run: pnpm --filter @reqtrack/worker test
