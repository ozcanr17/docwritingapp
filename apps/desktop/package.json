{
  "name": "@docsys/desktop",
  "version": "0.1.2",
  "private": true,
  "scripts": {
    "check": "node scripts/check-readiness.mjs",
    "dev": "tauri dev",
    "build": "tauri build --config src-tauri/tauri.local.conf.json",
    "build:binary": "tauri build --no-bundle",
    "build:debug": "tauri build --debug",
    "typecheck": "cargo check --manifest-path src-tauri/Cargo.toml",
    "icon": "tauri icon app-icon.svg"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.8.0"
  }
}
